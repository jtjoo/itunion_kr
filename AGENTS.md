# AGENTS.md — ITu (React Frontend)

Korean Information Technology Industry Labors Union (ITu / IT노조) website frontend. A standalone SPA built with Vite + React — **not** Next.js, and **not** the Payload CMS project in `itu-react/` (that is a separate, deprecated codebase).

## Tech Stack

- React 18 + TypeScript
- Vite 6 (build tool)
- React Router 7 (`createBrowserRouter`)
- Tailwind CSS 4
- Radix UI + shadcn/ui components
- Lucide React (icons)
- npm is the package manager (do **not** switch to pnpm)

## Commands

```bash
npm install        # install dependencies
npm run dev        # dev server at http://localhost:5173
npm run build      # production build to dist/
```

Docker (for deployment):

```bash
docker compose up -d   # build & serve via nginx at http://localhost:5173
docker compose down
```

## Project Structure

```
src/
├── main.tsx               # entrypoint (render <App /> into #root)
├── app/
│   ├── App.tsx            # root component (RouterProvider)
│   ├── routes.tsx         # all route definitions
│   └── components/
│       ├── layout.tsx     # shared header/footer + <Outlet />
│       ├── home-page.tsx  # / (page components are named exports)
│       ├── about-page.tsx
│       ├── news-page.tsx
│       ├── consult-page.tsx
│       ├── community-page.tsx
│       ├── promises-page.tsx
│       ├── figma/         # Figma-generated exports
│       └── ui/            # shadcn/ui components (accordion, button, dialog, ...)
├── imports/               # Figma Make generated components & SVG path maps
├── assets/img/            # bundled images
├── styles/                # index.css (Tailwind), fonts.css, theme.css
└── vite-env.d.ts
```

## Conventions

- **Pages**: defined in `src/app/routes.tsx` under the `Layout` route; each page component uses a named export (`export function HomePage()`).
- **Routing**: use `createBrowserRouter` + `Link`/`Outlet` from `react-router`. Deep links are handled by nginx SPA fallback (`try_files ... /index.html`) in `nginx.conf`.
- **Styling**: Tailwind utility classes only — no CSS modules. Use `@` alias for `src/` imports (configured in `vite.config.ts`).
- **Colors**: hardcoded hex via arbitrary values. Brand red is `#c30d23`; grays are `#1e1e1e`, `#757575`, `#d9d9d9`, `#e6e6e6`, etc. Match existing values rather than inventing new palettes.
- **Fonts**: Noto Sans KR via `font-['Noto_Sans_KR',sans-serif]`; display fonts `font-display-promise` / `font-display-safety` / `font-display-life` (April16th family, defined in `src/styles/fonts.css`).
- **Images**: SVG paths imported from `src/imports/svg-*.ts` (Figma-generated path maps). Bitmap assets live in `public/assets/img/` (referenced as `/assets/img/...`).
- **UI text**: Korean (ko). Keep UI strings in Korean.
- **Components**: shadcn/ui components in `src/app/components/ui/` are the standard UI primitives — reuse them instead of adding new ones.

## Gotchas

- Never add `.css`, `.ts`, or `.tsx` files to `assetsInclude` in `vite.config.ts` (comment in the file explains this).
- `dist/` is gitignored build output — do not commit it.
- `.env` contains secrets and is gitignored.
- `package-lock.json` is generated by npm; commit lockfile changes.
